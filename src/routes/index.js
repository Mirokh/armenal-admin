import {createRouter, createWebHistory} from 'vue-router'import BlogIndex from "@/views/blog/Index";import BlogForm from "@/views/blog/Form";import Gallery from "@/views/gallery/Index";import GalleryForm from "@/views/gallery/Form";import Settings from "@/views/Settings";import CareerIndex from "@/views/career/Index.vue"import CareerForm from "@/views/career/Form.vue"import Login from "@/views/auth/Login.vue";const routes = [  {    path: '/',    component: BlogIndex,    name: 'news'  },  {    path: '/news/form/:id?',    component: BlogForm,    name: 'news_form'  },  {    path: '/gallery',    component: Gallery  },  {    path: '/gallery/form/:id?',    component: GalleryForm,    name: 'gallery_form'  },  {    path: '/careers',    component: CareerIndex,    name: 'career'  },  {    path: '/careers/form/:id?',    component: CareerForm,    name: 'career_form'  },  {    path: '/settings',    component: Settings  },  {    path: '/login',    component: Login,    name: 'login'  }]const router = createRouter({  history: createWebHistory(process.env.BASE_URL),  routes})router.beforeEach(async (to, from, next) => {  const isAuthenticated = localStorage.getItem('auth');  if (!isAuthenticated && to.name !== 'login') {    await router.push({name: 'login'})    next(false);  } else if (isAuthenticated && to.name === 'login') {    await router.push({path: from.path});    next(false);  } else {    next(true)  }})export default router